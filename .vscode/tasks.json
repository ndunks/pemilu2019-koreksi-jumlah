{
    // install nodemon secara global `npm install -g nodemon`
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run-watch",
            "type": "shell",
            "windows": {
                "command": "nodemon.cmd -w koreksi.php --exec 'php koreksi.php -v'",    
            },
            "command": "nodemon -w koreksi.php --exec 'php koreksi.php -v'",
            "problemMatcher": []
        }
    ]
}